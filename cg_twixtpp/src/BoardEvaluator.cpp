#include "BoardEvaluator.hpp"

namespace agent {
    /* in progress
    void obstacleDistance(const Board& board) {
        std::queue<Position> Q;
        for (int r = 0; r < Board::kRows; r++) {
        for (int c = 0; c < Board::kCols; c++) {
            if ( peg)
        }
        }
    }
    */

    Score BoardEvaluator::evaluateBoardScore(const Board& board) {
    }
}